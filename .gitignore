# Environment variables (secrets / local overrides)
.env
.env.local
backend/.env
frontend/.env.local
# Public Vite build config (VITE_* only — safe to commit; never put secrets in VITE_*)
# frontend/.env.production is intentionally tracked
!frontend/.env.example

# Local security assessment artifacts
assets/red-team/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Database
*.db

# Node modules
node_modules/

# Vite build
frontend/dist/

# VS Code
.vscode/

# Claude context files
CLAUDE.md
tasks/
.claude/

# Runtime state
backend/quota.json
# machina cursor state (do not commit)
.machina/
